auth {
  mode: none
}

docs {
  Pressbin HTTP API collection.
  
  - **admin/** — `pb_admin_...` from `admin.key` (setup). Admin cannot sync.
  - **sync/** — `pb_sync_...` from `sync.key` (setup). Sync cannot access admin APIs.
}
