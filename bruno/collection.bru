headers {
  Authorization: Bearer pb_admin_ThmHwiy6Dvygb8tWEKyENPBuPiGP0RQd
}

auth {
  mode: bearer
}

docs {
  Admin API for Pressbin. Requires a `pb_admin_...` key (`Authorization: Bearer ...`).
  Set `adminKey` in the **local** environment (printed once on first server start).
}
